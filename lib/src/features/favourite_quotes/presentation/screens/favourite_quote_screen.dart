import 'package:flutter/material.dart';

class FaveroitQoutes extends StatelessWidget {
  const FaveroitQoutes({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(child: Text("Faveroit Qouts")),
    );
  }
}
